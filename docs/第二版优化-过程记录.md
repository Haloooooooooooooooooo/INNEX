# 第二版优化-过程记录

## 文档目的

这份文档用于记录第二版优化的实际推进过程，重点沉淀以下内容：

- 本轮优化具体做了什么
- 过程中出现了什么问题
- 遇到了哪些困难和风险
- 最后是怎么定位和解决的

这不是最终方案文档，也不是执行清单，而是偏实施过程的工作日志与复盘记录。

---

## 优化背景

第二版优化是在现有实现基础上做增量升级，不重建主骨架，继续沿用：

`capture -> parse -> internalize -> graph -> QA`

本轮先从 `Phase 1` 和 `Phase 2` 开始落地，核心目标是：

- 理顺数据与资产边界
- 补强原文资产与切片基础设施
- 让录入、解析、内化的链路在不推翻现有代码的前提下变得更稳

---

## 本轮已经完成的主要工作

## 1. 新增原文来源资产与原文切片基础设施

已新增 migration：

- [009_add_capture_item_sources_and_source_chunks.sql](/e:/my_vibecoding/INNEX/apps/web/supabase/migrations/009_add_capture_item_sources_and_source_chunks.sql)

主要内容：

- 新建 `capture_item_sources`
- 新建 `source_chunks`
- 增加索引与 RLS
- 增加原文切片检索 RPC：
  - `match_source_chunks`
  - `match_source_chunks_in_capture_items`

目标：

- 把“原文资产”从只有 `capture_items.raw_content` 的粗粒度形态，提升到“按来源保存 + 可切片检索”的基础设施形态

---

## 2. 录入阶段开始保存来源级原文

已修改：

- [capture-items/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/capture-items/route.ts)
- [capture-items/[id]/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/capture-items/[id]/route.ts)

主要工作：

- 新增 `SourceDraft` / `ExtractedFileSource`
- 在录入阶段把以下内容尝试按来源保存：
  - 用户输入
  - URL 正文
  - 附件提取文本
  - 图片 OCR 文本
  - 用户理解
  - `raw_content` fallback
- `GET /api/capture-items` 和 `GET /api/capture-items/[id]` 增加 `sources` 读取能力

目标：

- 让 `capture_items` 不再只是顶层主视图，同时具备来源级原文资产入口

---

## 3. 内化阶段开始优先消费来源级原文

已修改：

- [internalize/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/internalize/route.ts)

主要工作：

- 新增来源级原文读取逻辑
- 新增原文上下文组织逻辑
- 内化时优先使用来源级原文，而不是只依赖 `raw_content`
- 在内化阶段补建 `source_chunks`

目标：

- 让内化阶段逐步过渡到“以原文资产为底座”的工作方式

---

## 4. 修复 parse 阶段 summary 超时过于激进的问题

已修改：

- [generator.ts](/e:/my_vibecoding/INNEX/apps/web/lib/parse/generator.ts)

主要工作：

- `summary` 阶段超时与重试从默认较短配置提升为：
  - `attempts: 3`
  - `timeoutMs: 25000`
  - `retryDelayMs: 700`
- 为 `summary` 增加启发式 fallback
- 即使模型超时，也尽量返回一个可用的录入摘要

问题来源：

- 录入日志中频繁出现：
  - `phase: 'summary'`
  - `normalized_reason: 'model_timeout'`
  - `raw_error: 'timeout_after_12000ms'`

解决结果：

- parse 摘要不再过于依赖短超时模型响应
- 录入阶段在模型抖动时仍尽量有摘要可用

---

## 5. 修复 parse prompt 乱码问题

已修改：

- [prompts.ts](/e:/my_vibecoding/INNEX/apps/web/lib/parse/prompts.ts)

问题表现：

- 旧的 parse prompt 文件存在明显乱码
- 这会直接影响 title / summary / tags 的生成质量

处理方式：

- 直接重写 `title / summary / tags` 的 parse prompts
- 保持要求明确、简短、可控

解决结果：

- 模型拿到的 parse 指令恢复正常
- 为后续摘要和标签质量提升打下基础

---

## 6. 收紧录入摘要输出格式

已修改：

- [generator.ts](/e:/my_vibecoding/INNEX/apps/web/lib/parse/generator.ts)

问题表现：

- 对 Markdown / 结构化文档录入时，摘要经常过长
- 甚至会把：
  - `# 标题`
  - `> 引用`
  - 分节结构
  - 大段原文
 直接带进 summary

处理方式：

- 新增 markdown 清洗逻辑
- 新增摘要文本归一化逻辑
- 强制 summary 更接近：
  - `1-2` 句
  - 纯文本
  - 无 markdown
  - 长度受控

解决结果：

- 录入摘要更像“摘要”
- 而不是“原文开头节选”

---

## 7. 收紧 tags 输出，减少目录词和结构词

已修改：

- [generator.ts](/e:/my_vibecoding/INNEX/apps/web/lib/parse/generator.ts)

问题表现：

- 对 Markdown / 文档类内容，tags 容易出现：
  - `第一章`
  - `本章`
  - `章节`
  - `部分`
  - `核心论点`
 这类结构词、泛词、目录词

处理方式：

- 扩充 tag generic blacklist
- 归一化前先清洗 markdown
- 过滤常见章节编号与结构型标签

解决结果：

- tags 更偏主题词
- 少一点“目录感”和“排版感”

---

## 8. 实现小红书图文链接按图片数量分流

已修改：

- [parse-url/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/parse-url/route.ts)
- [capture-items/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/capture-items/route.ts)
- [internalize/route.ts](/e:/my_vibecoding/INNEX/apps/web/app/api/internalize/route.ts)

实现目标：

- 针对“小红书图文笔记文字很少、干货主要在图片中”的场景补一条最小可用链路

本次落地规则：

- 如果识别到小红书链接图片数 `<= 5`
  - 录入阶段直接对全部图片做 OCR
  - 让 summary / tags / raw_content 尽量同时吸收文字和图片内容

- 如果识别到小红书链接图片数 `> 5`
  - 录入阶段不全量 OCR
  - 只保留页面文字、图片数量、deferred 标记和图片 URL 列表
  - 把全量图片 OCR 延后到内化阶段

录入阶段具体做法：

- `parse-url` 新增图片 URL 提取能力
- 返回：
  - `image_urls`
  - `image_count`
  - `platform`
- `capture-items` 根据图片数分流：
  - 小于等于 5 张时，抓远程图片并 OCR
  - 大于 5 张时，写入 deferred 标记

内化阶段具体做法：

- 如果 `parse_debug` 中包含：
  - `url_platform = xiaohongshu`
  - `image_ocr_deferred = true`
  - `deferred_image_urls`
- 则在内化前先把远程图片 OCR 成 `capture_item_sources`
- 再进入原有内化流程

阶段性结果：

- 小红书图文已不再只能“读链接文字”
- 系统开始具备“少图录入即识别、多图延后内化补全”的分流能力

---

## 过程中遇到的主要问题

## 1. `capture_item_sources` 新能力接入后，Supabase schema cache 未及时识别

实际日志表现：

- `sources-persist-failed`
- `reload-failed`
- `Could not find a relationship between 'capture_items' and 'capture_item_sources' in the schema cache`

问题本质：

- migration 文件已经在本地代码中存在
- 但远端数据库或 PostgREST schema cache 未及时反映新表/关系

这导致：

- `capture_item_sources` 插入不稳定
- `capture_items` reload 时带 `sources:capture_item_sources(*)` 查询报错

应对策略：

- 不让主链路因为新能力未就绪而失败
- 对 `sources` 相关查询和写入做向后兼容降级

处理结果：

- 即使 schema cache 未刷新，录入主流程仍可继续
- `sources` 能力暂时缺失时，系统自动 fallback 到旧行为

---

## 2. 错误日志一度只显示 `[object Object]`

问题表现：

- `sources-persist-failed` 的 error 输出不可读

困难：

- 这样很难快速判断到底是：
  - 表不存在
  - schema cache 问题
  - 关系不可见
  - 还是别的 RLS/SQL 错误

处理方式：

- 增加 Supabase 错误对象格式化逻辑

结果：

- 错误日志变得更可读
- 后续排查 schema cache 问题更容易

---

## 3. 文档类录入的 summary 质量不稳定

问题表现：

- 有时超时
- 有时太长
- 有时直接把 Markdown 结构带进来

困难：

- 这里不是单一问题，而是多个因素叠加：
  - prompt 乱码
  - 超时太短
  - 缺少后处理
  - fallback 太原始

处理方式：

- 修 prompt
- 放宽 summary retry/timeout
- 增加摘要清洗
- 增加 heuristic fallback

结果：

- 录入阶段摘要已明显收紧
- 但后续还需要继续观察实际样本质量

---

## 本轮采用的总体处理策略

这次优化没有选择“推倒重来”，而是遵守了以下策略：

1. 只做增量增强
- 在现有对象和 API 上扩展
- 不另起平行系统

2. 优先保证主链路稳定
- 新能力未完全就绪时，允许 graceful fallback
- 不因为 `sources` 基础设施未准备好就让录入失败

3. 把错误分层处理
- 模型慢：放宽 timeout + fallback
- prompt 脏：修 prompt
- schema cache 未刷新：降级兼容
- 输出太长：后处理清洗

4. 每改一轮都做构建验证
- 多次执行 `apps/web` 下的 `npm.cmd run build`
- 当前相关改动均已通过构建

---

## 当前仍存在的未完全闭环问题

## 1. Supabase schema cache 是否已刷新仍需确认

虽然代码层已经兼容，但如果要真正启用 `capture_item_sources` 和 `source_chunks`，仍需要确认：

- migration 已执行到目标 Supabase 环境
- PostgREST schema cache 已刷新

建议动作：

- 在 Supabase SQL Editor 执行：

```sql
NOTIFY pgrst, 'reload schema';
```

- 然后重新验证 `capture_items` 查询是否还能报 relationship 错误

---

## 2. parse 阶段质量还需要继续实测

目前已经修复：

- prompt 乱码
- summary 超时
- summary 过长
- tags 结构词污染

但还需要继续用真实录入样本验证：

- md 文档
- pdf 文档
- OCR 图片
- URL 长文

---

## 3. Phase 1 / Phase 2 的“能力已落地”和“完全启用”不是一回事

目前状态更准确地说是：

- 代码层：Phase 1 / Phase 2 已基本落地
- 环境层：仍需确认 Supabase 远端 schema 与 cache 状态

也就是说：

- 兼容逻辑已经具备
- 新资产层已经写进代码
- 但远端环境若未同步完成，部分增强能力会先降级运行

---

## 本轮工作的阶段性结论

第二版优化的前两阶段已经进入“代码已落地、环境待完全确认”的状态。

这一轮最大的价值不是“所有新能力一次性全部完美启用”，而是：

- 先把原文资产层与原文切片基础设施植入现有系统
- 先把 parse 链路里最明显的不稳定点修掉
- 先让系统在新旧能力并存时也能稳定工作

这为后续继续推进：

- `Phase 3：内化层升级`
- `Phase 4：图谱层增强`
- `Phase 5：QA 检索与 Agent 决策升级`

打下了更稳的基础。

---

## 后续建议

下一步建议按这个顺序继续：

1. 先确认 Supabase 远端 migration 与 schema cache 状态
2. 继续用真实样本验收 Phase 1 / Phase 2
3. 验收通过后，再进入 Phase 3

---

## 2026-05-29 补充复盘：小红书图文链路（Phase 2）收敛结果

### 本轮进度
- 已打通「小红书链接 -> 图片抓取 -> OCR -> 入库 sources -> 聚合到 raw_content」链路。
- 最终验收样本显示：
  - `url_platform = xiaohongshu`
  - `url_image_count = 4`
  - `inline_image_ocr_count = 4`
  - `notes` 包含 `xhs_image_ocr_succeeded:4`、`xhs_external_crawler_used:4`
- 说明：本地外部爬虫回退链路生效，且已能稳定拿到图文图片并完成 OCR。

### 遇到的主要困难
- 1) 小红书反爬与登录墙：
  - 直接页面抓取时经常拿到登录页文案，或拿不到真实图片 URL。
- 2) 候选图片 URL 噪音高：
  - 出现大量 `application/javascript` / `text/css` 伪图片链接。
- 3) OCR 模型兼容与稳定性：
  - 部分模型对图片请求返回 400，导致批量 OCR 失败。
- 4) OCR 内容重复严重：
  - 识别结果中出现大量循环行，导致 `raw_content` 膨胀、噪音高。

### 解决方案（已落地）
- 1) 三段式抓取回退：
  - 静态提取 -> 渲染提取 -> 外部 crawler 接口（本地可用，后续可替换部署版）。
- 2) 平台识别与登录墙处理：
  - 对 xiaohongshu 强制平台识别；
  - 对登录墙 OCR 结果识别并丢弃（避免脏数据入库）。
- 3) 远程图片抓取增强：
  - 强化请求头、403 重试、content-type 校验、大小限制。
- 4) OCR 结果清洗去重：
  - 增加轻量 normalize：
    - 去连续重复行；
    - 同一行全局限次；
    - 压缩短周期循环段；
  - 多图 OCR 块去重后再拼接到 `raw_content`。
- 5) 内部接口鉴权修复：
  - `capture-items` 调用 `parse-url` 时透传 cookie，修复 401。

### 可复用经验（建议沉淀为团队规范）
- 1) 反爬平台接入必须设计「多级回退」，不要押注单一抓取方式。
- 2) URL 候选必须先做内容类型过滤，再进 OCR；否则成本高且噪音大。
- 3) OCR 不是“识别完就用”，必须配套清洗与去重层。
- 4) 新能力上线时优先「可降级」：
  - schema/cache/外部依赖异常时，主链路仍要可用。
- 5) 验收标准要数据化：
  - 以 `parse_debug` 的关键字段作为通过条件（平台、图片数、OCR 成功数、回退链路标记）。

### 当前状态判断
- Phase 2（小红书图文原文资产采集）已达到“可用且可验收”的状态。
- 仍建议在 benchmark 样本池上继续跑 1~2 轮稳定性回归（不同链接、不同图数、不同网络条件）。
- 可进入 Phase 3（内化层升级），同时保留本链路的灰度观察与日志监控。

---

## 2026-05-29 补充复盘：Phase 3（内化层升级）收敛结果

### 本轮进度
- 已完成“无原文不内化”的硬约束：
  - 内化阶段若无法拿到 primary source（`raw_content + 附件抽取文本`），直接 422 阻断。
- 已完成内化阶段附件抽取增强：
  - parser-service 解析失败时，增加 URL 拉取与 Supabase Storage 下载双通道兜底。
- 已完成 dry-run 行为修正：
  - `dry_run=true` 也执行真实原文抽取，不再出现“诊断为空”。
- 已完成内化状态会话化：
  - 启动内化后关闭抽屉不取消流程；重开同记录自动回到内化页。
- 已完成内化结果回写增强：
  - 保存后即时同步 `status/summary/tags` 与 AI 笔记内容，减少“保存后无变化”错觉。

### 遇到的主要困难
- 1) 旧兜底路径会“伪成功”：
  - 无原文时仍可生成通用笔记，影响数据可信度。
- 2) 状态一致性复杂：
  - 列表、详情、草稿、后台异步提交存在时序竞争。
- 3) 附件可读路径不稳定：
  - `storage_path` 可能是公网 URL、对象路径或临时不可访问路径。
- 4) 标签生成条件误判：
  - `['-']` 被视为已有标签，导致内化阶段不触发标签生成。

### 解决方案（已落地）
- 1) 强制原文门禁：
  - 新增最小原文长度阈值，未达到则返回 `INTERNALIZE_NO_CONTENT`。
- 2) 抽取诊断可观测化：
  - `blocked_no_primary_source` 日志新增 `attachment_diag`，细化到 parser、URL fetch、storage download、extract 各步骤。
- 3) dry-run 与正式内化一致化：
  - dry-run 不再跳过附件解析，预览结果与正式执行口径一致。
- 4) UI 会话化收敛：
  - 内化会话期间列表按钮持续显示“内化中”；关闭后重开自动回到内化页；仅“返回详情/保存完成或失败”结束会话。
- 5) 标签/摘要补全策略修正：
  - 仅在空值场景生成；并把 `- / — / 暂无标签` 归为无效标签；模型失败时启发式兜底补全。

### 可复用经验（建议沉淀为团队规范）
- 1) “可用”不等于“可信”：涉及知识资产入库，必须先保证原文可追溯。
- 2) 内化类功能应采用“会话化状态机”而非单点按钮状态。
- 3) 解析链路必须有分步骤诊断字段，否则无法高效定位根因。
- 4) 占位值（如 `-`）必须纳入业务语义校验，否则会误伤后续流程。
- 5) dry-run 与正式执行必须同口径，避免预览与落库行为分叉。

### 验收口径（本轮建议）
- 1) `no_source_but_success_rate = 0`
- 2) `internalize_parse_success_rate >= 98%`
- 3) 内化会话恢复成功率（关闭后重开）= 100%
- 4) 有效标签覆盖率（排除占位值）>= 98%
- 5) 关键链路日志具备可定位字段（`parse_status/parse_error_code/attachment_diag`）

### 当前状态判断
- Phase 3 已进入“能力可用 + 规则收敛”的状态。
- 仍建议在 benchmark 样本池上继续做 1~2 轮稳定性回归（重点覆盖：URL 无正文、私有附件、大 PDF、低质量 OCR）。
- 在 QA 全量优化前，可先冻结 Phase 3 规则，进入“稳定性验证与 badcase 回灌”阶段。

## 2026-05-29 补充复盘：Phase 4（图谱层增强）第一轮落地
### 本轮进度
- 已把图谱主节点固定为 `notes`，并移除 `/api/graph` 的 `entity` 输出分支，前端图谱页同步取消“实体图谱”切换。
- 已统一关系类型白名单为最小稳定集合：`related / supports / example_of`。
  - 覆盖接口：`/api/note-relations`、`/api/note-relations/batch`、图谱前端颜色映射与 TS 类型定义。
- 已在图谱读取侧显式过滤旧关系类型（如历史 `contradicts`），确保当前阶段只消费最小关系集。
- 已在内化产边链路补齐边证据字段：
  - `relation_type`
  - `confidence`（分层可解释）
  - `evidence.evidence_summary`
  - 并补充 `shared_concepts / overlap_count / similarity` 等可回溯信号。
- 已移除 QA 中对 `contradicts` 的依赖性判断，避免图谱阶段目标与 QA 侧逻辑冲突。

### 本轮采用的图谱关系规范（Phase 4 验收输出）
1. 关系类型规范
- 当前仅允许三类关系：`related`、`supports`、`example_of`。
- 不引入 `contradicts`，避免在证据体系与冲突判定策略尚未完备前放大误判风险。

2. 关系生成规则（V1）
- 三段式固定流程：
  - 粗筛召回：embedding 相似度召回候选 note。
  - 判型：结合 tags/concepts overlap 做轻判型（当前自动产边主要为 `related / supports`）。
  - 证据落库：将 confidence 与 evidence_summary 一并入库。
- 主输入优先：note 摘要、标签/概念、事实片段。
- 辅助输入：正文与原文片段（由上游内化资产组织提供）。
- `my_understanding` 与推断层内容不作为主判定依据。

3. 边证据与置信度规则（V1）
- 每条自动关系边至少包含：
  - `relation_type`
  - `confidence`
  - `evidence.evidence_summary`
- 置信度采用分层解释，不追求过度精细数值：
  - 高：`>= 0.75`
  - 中：`0.60 ~ 0.75`
  - 低：`< 0.60`
- evidence_summary 优先引用：共享概念、事实片段线索、原文支持片段索引、简短判定理由。

4. 与 QA 的结构价值确认
- 图谱边已可直接作为 QA 的结构化扩展信号来源（关系类型 + 置信度 + 证据摘要）。
- 当前定位明确：图谱是“关系导航层”，不是“事实替代层”。

### 本轮仍保留的后续增强项
- 关系“模型判型”目前为轻量规则增强（embedding + overlap），下一轮可引入专门 relation-classifier prompt 进一步提升 `supports/example_of` 判型准确率。
- 可在 benchmark 中新增 phase4 指标：
  - `relation_type_precision`
  - `evidence_summary_usable_rate`
  - `graph_relation_qa_reuse_rate`

## 2026-05-29 补充记录：Phase 4 benchmark口径固化
- 已更新 benchmark 设计文档，新增 Phase4 图谱专项评测口径：
  - 关系类型范围（仅 `related / supports / example_of`）
  - 边证据完整度（`relation_type + confidence + evidence_summary`）
  - 置信度分层校验（高/中/低）
- 已更新 benchmark 结果记录表：
  - 新增图谱失败原因标签：
    - `graph_relation_type_out_of_scope`
    - `graph_evidence_incomplete`
    - `graph_confidence_misaligned`
    - `graph_evidence_summary_weak`
  - 新增图谱记录模板字段：关系类型分布、证据完整度、证据摘要可用性。
- 目的：让 Phase4 后续回归从“主观判断”转为“可重复、可对比、可汇总”的同口径评测。

## 2026-05-29 追加修复：图谱出现“有节点无边”
### 现象
- 全量重内化后，图谱页面节点正常显示，但边数量为 0。

### 根因判断
- 内化建边链路当前优先依赖 embedding 召回（`match_note_chunks`）。
- 在 embedding 缺失、召回为空、或阈值较高场景下，原逻辑会非阻断继续，最终可能没有任何关系边产出。

### 已落地修复
- 在 `internalize` 的 `build_relations` 阶段新增“非向量兜底建边”逻辑：
  - 当 embedding 召回未产边时，改用结构化重叠信号补边：
    - tags/concepts overlap
    - title/summary keyword overlap
  - 兜底产边仍写入 Phase4 证据字段：
    - `relation_type`
    - `confidence`
    - `evidence.evidence_summary`
  - 关系类型仍限定在当前稳定集合：`related/supports/example_of`（兜底当前产 `related/supports`）。

### 结果
- 即使 embedding 链路暂时不可用，也能避免“图谱完全断开”。
- 构建通过（`npm run build`）。

### 后续建议
- 继续观察：若 embedding 恢复，应优先使用向量召回结果；兜底仅作为保底路径。
- 在 benchmark 结果表中，单独记录是否触发 `structured_overlap_fallback`，便于统计真实向量链路稳定性。

## 2026-05-29 追加调整：单条内化最大建边数提升
- 调整目标：解决“每条新 note 仅最多建 5 条边”导致的关系覆盖不足。
- 代码调整：`internalize` 建边上限由 5 提升为 30，统一作用于：
  - embedding 召回 `match_count`
  - 主建边循环提前停止条件
  - 非向量兜底建边候选截断
- 结果：单条内化可与更多历史 note 建立关系，提升图谱连通性与后续 QA 可扩展性。

## 2026-05-29 追加升级：更科学的内化建边主链路
- 已将内化建边主链路从“仅首个chunk召回”升级为“前N个chunk融合召回（默认3）”。
- 召回参数改为可配置：
  - `INTERNALIZE_RELATION_RECALL_CHUNK_COUNT`（默认 3，范围 1~5）
  - `INTERNALIZE_RELATION_MATCH_THRESHOLD`（默认 0.62，范围 0.3~0.95）
  - `INTERNALIZE_MAX_RELATIONS_PER_NOTE`（默认 30，范围 1~50）
- 召回融合规则：同一候选 note 多次命中时保留最高 similarity，最后按相似度排序截断。
- 边证据增强：写入 `source_chunk`、`recall_chunk_count`、`match_threshold` 到 evidence，便于回溯判定来源。
- 可观测性增强：`[internalize] success` 日志新增 `relation_recall_chunk_count`、`relation_match_threshold`、`relation_max_per_note`。
- 构建验证通过（`npm run build`）。

## 2026-05-29 追加修复：图片录入“无响应”卡住问题
### 现象
- 图片录入时前端长时间等待，无明显报错，表现为“无响应”。

### 排查结论
- 本轮 phase4 主改动未触达 `capture-items` 录入链路（改动集中在 `internalize` 图谱建边）。
- 但图片 OCR/视觉调用底层 `runVisionPrompt` 之前缺少超时控制，网络抖动或模型接口挂起时会长期 pending，造成录入无响应。

### 已修复
- 在 `apps/web/lib/llm/client.ts` 的 `runVisionPrompt` 增加请求超时与中断：
  - 新增 `OCR_REQUEST_TIMEOUT_MS`（默认 25000ms）。
  - 每次模型尝试都使用 `AbortController` 控制超时。
- 失败策略改为“继续尝试下一候选模型”，而不是单次异常直接终止。

### 结果
- 录入图片场景不再无限等待，超时会快速失败并进入 fallback/错误路径。
- 构建验证通过（`npm run build`）。

## 2026-05-29 阶段结论更新：Phase 4 已实现（可用）
### 当前结论
- Phase 4（图谱层增强）已达到“实现并可用”状态。
- 现状不是“方案层讨论”，而是“链路可跑、结果可见、可评估可迭代”。

### 本轮闭环（关键问题 -> 根因 -> 修复）
1) 现象：图谱有节点但无边
- 根因：内化建边对向量召回依赖较强，召回为空时容易出现零边；同时早期参数偏紧。
- 修复：
  - 建边主链路升级为“前3个chunk融合召回”（默认，可配置）。
  - 单条内化最大建边上限提升到 30（可配置）。
  - 结构化兜底与冷启动兜底补边，避免图谱完全断开。

2) 现象：日志显示 edge_count>0，但前端不显示边并出现 G6 告警
- 根因：前端把业务关系类型（related/supports）错误复用到 G6 edge `type`（渲染类型）字段。
- 修复：
  - 业务关系改用 `relationType`。
  - G6 渲染 `type` 固定为 `line`。

3) 现象：图片录入偶发“无响应”
- 根因：视觉/OCR 请求缺少超时控制，网络抖动时可能长时间 pending。
- 修复：
  - OCR 视觉请求增加超时中断与候选模型继续重试。

### 能力状态（Phase4）
- 图谱主节点固定为 `notes`。
- 当前关系集合固定为：`related / supports / example_of`。
- 图谱边已包含：`relation_type / confidence / evidence_summary`。
- 图谱边可作为 QA 扩检索的结构化输入使用。

### 实施口径
- 阶段判定：`已实现（可用）`。
- 同时保留：`持续优化中`（重点是关系准确率与覆盖率，不是可用性缺失）。

### 建议下一步（执行顺序）
1. 按 benchmark 跑一轮“修复后”结果回填
- 优先样本：S02/S03/S04（强相关组）+ S08/S10（中相关组）。
- 回填字段：关系类型分布、证据完整度、证据摘要可用性。

2. 进入 Phase 5（QA 检索与决策升级）
- 先固化检索顺序：内化资产 -> 原文资产 -> 图谱扩检索。
- 把图谱关系用于“按意图扩检索偏好”而非首跳入口。

3. 保持参数冻结一轮
- 在本轮 benchmark 完成前，避免频繁继续调阈值；先用同口径验证当前稳定性。

## 2026-05-29 追加调整：关系质量收敛与图谱交互美化
### 关系生成策略调整
- 按产品要求取消“强行关联”倾向：
  - 移除冷启动最近笔记强制建边兜底。
  - 结构化兜底阈值恢复为 `score >= 3`，仅在有足够证据时产边。
- 目标：减少误连，保证边语义可信度。

### 图谱可视化交互增强
- 关系分色：按 `related / supports / example_of` 区分基础色。
- 强度表达：同类型关系按 `confidence` 做透明度渐变，强关系更醒目、弱关系更柔和。
- 悬浮高亮：鼠标悬浮某节点时，和该节点有关联的节点与边高亮，其他元素自动降噪。
- 技术实现：将业务关系类型与 G6 渲染类型解耦（`relationType` vs `type=line`）并在绘制数据中注入高亮样式字段。

### 结果
- 保持 Phase4 关系规范前提下，提升可解释性与可读性。
- 构建验证通过（`npm run build`）。

## 2026-05-29 第一轮优化方案已落地（基于 S02/S03/S04 图谱复盘）
### 本轮改动
1) concepts 二次兜底
- 当首次概念抽取结果不足（<3）时，触发第二次抽取（基于标题+正文）。
- 概念统一走 `normalizeConcepts`，过滤空值、占位词、过长词、重复词。

2) 轻量乱码清洗
- 新增 `cleanMojibakeLite`，在 summary/tags/concepts 入库前做轻清洗（不改语义，只清异常符与脏空白）。

3) 关系判型补 `example_of`
- 新增 `inferRelationTypeLite`：
  - 有“例如/案例/模板/example/case”等信号且有重叠时，判 `example_of`；
  - 否则按 overlap 判 `supports/related`。

4) 建边诊断日志增强
- 新增 `[internalize] relation_diagnostics`：
  - `embedding_recall_candidates`
  - `fallback_used`
  - `final_relations_count`
- 用于区分“主向量链路产边”与“fallback 产边”。

### 状态
- 构建验证通过（`npm run build`）。
- 可进入第二次评测（同样本 S02/S03/S04 对比第一轮）。

## 2026-05-29 第一轮优化方案补充落地（知识体系优先）
- 关系构建从“仅强相关”扩展为“强+中相关”分层保留：
  - fallback 阈值由 `score>=3` 调整为在补充阶段允许 `score>=2`，并在未超上限时增量补边。
  - 仍保留证据门槛，不回到强制冷启动连边。
- 图谱展示新增“最低置信度过滤”控件：
  - 可切换 `显示全部 / >=0.50 / >=0.60 / >=0.75`。
  - 让存储层尽量保留知识网络，展示层按需要控噪。
- 关系判型继续使用轻规则并引入 `example_of` 信号；关系诊断日志持续输出。
- 构建验证通过（`npm run build`）。

## 2026-05-29 追加：KB 抽屉字段与 Inbox 详情页 1:1 对齐
- 目标：知识图谱右侧抽屉与 Inbox 详情字段保持一致，减少两套信息口径差异。
- 改动文件：`apps/web/app/kb/page.tsx`
- 已落地：
  - 抽屉元信息新增并对齐：`来源`、`收录时间`、`状态(StatusBadge)`、`标签`。
  - 新增 `查看原笔记` 按钮：优先打开 `capture_item.source_url`，其次 `note.source_url`。
  - 抽屉详情优先展示 `capture_item` 信息（标题/来源/状态/标签），无 `capture_item` 时回退 `note` 字段。
  - 在节点详情请求后，按 `capture_item_id` 补拉取 `/api/capture-items/[id]`，用于字段对齐与状态展示。
- 兼容策略：
  - 若无外链来源，按钮给出明确提示，不会静默无响应。
  - 若 `capture_item` 拉取失败，自动回退到 `note` 字段展示，避免抽屉空白。
- 验证：`apps/web` 执行 `npm run build` 通过。

## 2026-05-29 追加修复：登录 `Failed to fetch`（AuthProvider）
- 现象：`providers/auth-provider.tsx` 在 `supabase.auth.signInWithPassword` 抛出 `Failed to fetch`。
- 处理：将前端 `signIn/signUp` 从浏览器直连 Supabase 改为调用站内接口：
  - `POST /api/auth/login`
  - `POST /api/auth/register`
- 目的：避免浏览器侧直连 Supabase 的网络/拦截不稳定，统一走同源请求链路。
- 兼容：新增 `try/catch` 网络异常兜底，返回明确提示“网络连接失败，请检查网络后重试”。
- 代码位置：`apps/web/providers/auth-provider.tsx`
- 验证：`apps/web` 执行 `npm run build` 通过。

## 2026-05-29 追加：KB 交互与加载反馈优化（选中态/悬浮态/加载动画）
- 针对问题 1（关闭详情后无选中感）：
  - 保持 `activeNoteId` 不清空，并把选中态改为“形态变化”而非换色，关闭抽屉后仍可明显识别。
- 针对问题 2（选中态样式）：
  - 节点选中时：变大 + 外层同色光圈（shadow）+ 黑色环（stroke），不再改节点主色。
- 针对问题 3（点击响应与加载态）：
  - 点击节点即刻打开抽屉并进入加载态，先反馈再异步拉取详情。
  - 新增统一橙色波浪加载组件 `WaveLoader`：
    - 图谱加载中（KB）
    - 列表加载中（Inbox）
    - 内化详情加载中（Inbox 草稿阶段）
- 针对问题 4（悬浮高亮不明显）：
  - 增强悬浮逻辑：悬浮节点及其关联节点颜色加深，关联边显著加粗加亮，其他边明显降噪。
  - 补充 `node:pointerenter/leave` 事件兼容，提升悬浮命中稳定性。
- 变更文件：
  - `apps/web/app/kb/page.tsx`
  - `apps/web/components/shared/wave-loader.tsx`（新增）
  - `apps/web/app/globals.css`
  - `apps/web/components/inbox/inbox-table.tsx`
  - `apps/web/components/inbox/inbox-drawer.tsx`
- 验证：`apps/web` 执行 `npm run build`。
- 2026-05-29 追加微调：图谱默认（非悬浮）边可见性增强。
  - `edgeOpacity`：0.18 -> 0.52
  - `edgeLineWidth`：1.0 -> 1.8
  - 目标：未悬浮时边不再过浅，保证基础可读性；悬浮高亮对比逻辑保持不变。
- 2026-05-29 追加微调：加载动画改为单行并适配容器。
  - `WaveLoader` 改为单行 8-block 波浪，不再 4x4 矩阵。
  - 新增 `fullHeight` 参数，支持在大容器内自动垂直居中。
  - block 尺寸与间距按 `sm/md` 响应式缩放，适配不同画面大小。
  - KB 图谱加载态已切换为 `fullHeight` 居中展示。
- 2026-05-29 追加：KB 抽屉关闭行为调整为“清空选中态”。
  - 关闭详情抽屉时同时清理 `selectedNoteId/activeNoteId/activeNote/activeCaptureItem`。
  - 效果：关闭后图谱恢复到无节点被选中的初始状态。
- 2026-05-29 追加：KB 详情页加载态视觉调整。
  - 详情加载中的 `WaveLoader` 调整为大号并在抽屉区域全高居中显示。
  - 实现：`fullHeight + min-h[320px]`，替代原先小号顶部加载。
- 2026-05-29 追加：悬浮高亮策略调整为“仅增强相关，不压暗无关”。
  - 悬浮节点时：相关边继续加亮加粗；无关边保持默认可见度，不再降到几乎消失。
  - 实现：hover 分支下无关边 `edgeOpacity/edgeLineWidth` 与默认值一致。
- 2026-05-29 追加：图谱“加载中”动画放大。
  - `WaveLoader` 新增 `size="xl"` 规格（方块、间距、文字同步放大）。
  - KB 图谱加载态切换为 `size="xl"`，并提升最小展示高度以强化视觉中心感。
- 2026-05-29 追加：KB 详情抽屉新增“双按钮 + AI视图”能力。
  - 新增按钮：`查看原笔记`、`查看AI笔记`。
  - `查看原笔记` 行为对齐 Inbox 列表：优先打开链接；文本记录打开原文视图；附件有可访问地址则直接打开。
  - `查看AI笔记`：切换到 AI 正文视图，并提供 `返回详情` 按钮。
  - 抽屉关闭或切换 note 时重置为详情视图，避免状态残留。
- 2026-05-29 追加：KB AI笔记视图样式修正。
  - 仅保留内层滚动：AI视图外层改为 `overflow-hidden`，正文框内滚动。
  - 滚动条样式对齐 Inbox 列表：AI 正文框使用 `list-scrollbar`。
  - AI 正文改为 markdown 风格渲染（标题/列表/引用/段落）。
  - AI正文保持“卡片框”展示，并在抽屉底部留出间距（`pb-4`）。
  - “返回详情”按钮改为橙色主按钮样式。
- 2026-05-29 追加：KB 详情页尺寸放大。
  - 抽屉宽度：`w-[360px] -> w-[460px]`。
  - 图谱/详情区域最小高度：`min-h-[460px] -> min-h-[560px]`，提高可用纵向空间。
  - 页面底部留白：`pb-8 -> pb-4`，进一步释放高度。
- 2026-05-29 追加：内化草稿页加载态收敛。
  - `draftLoading` 时改为单一加载视图：仅显示“内化内容生成中”。
  - 加载动画改为超大号并居中（`WaveLoader size=xl` + 垂直水平居中）。
  - 移除同页重复加载提示（“内化中...”“正在内化并生成草稿...”等）。
- 2026-05-29 追加：图谱节点颜色改为“增量分配并锁定”。
  - 问题：新增节点后全图重算颜色，导致已有节点变色。
  - 修复：引入节点颜色档案 `nodeColorProfileRef`（family+tier）。
  - 规则：
    - 已有节点颜色一旦分配即锁定，不再随新节点重算。
    - 新节点优先继承已有关联邻居的主色系；若无邻居则按色系轮转分配。
  - 效果：新增节点时，只有新节点可能出现新颜色，已有节点颜色保持不变。
- 2026-05-29 诊断结论：图谱关系召回偏低的主要原因并非单纯模型弱，而是当前建边策略偏保守。
  - 当前建边核心：向量召回 + 结构重叠兜底。
  - 主要参数：`INTERNALIZE_RELATION_MATCH_THRESHOLD=0.62`、`INTERNALIZE_RELATION_RECALL_CHUNK_COUNT=3`、fallback 粗筛 `score>=2`。
  - 当前语义短板：结构重叠主要靠 tags/concepts 的“字面匹配”，尚未做同义归一（如“prompt/提示词/提示语”），导致语义近邻漏连。
  - 风险判断：现状更像“策略阈值 + 特征工程不足”而不是“模型完全不够”。
- 2026-05-29 决策建议：引入大模型做关系语义判定（面向图谱+QA联动）。
  - 结论：建议引入。当前轻规则判型在跨表达语义（如 prompt/提示词）场景召回不足。
  - 与 QA 的关系：建议“同轮设计、分步落地”。
    1) Phase A（先落地图谱）：大模型 relation-classifier + 证据摘要标准化。
    2) Phase B（联动 QA）：QA 检索层优先消费图谱关系（关系类型/置信度/证据），用于扩检索与答案组织。
  - 工程策略：先给 QA 预留口子（relation evidence schema + retrieval hook），不必等 QA 全改完再做图谱。

---

## 2026-05-29｜Phase5 启动确认：关系判型模型与实现策略

本次与你确认后的执行口径：

1. 关系判型主模型：deepseek-v4-pro（主链路）。
2. 第一版策略：保守模式（证据不足时不强行建边）。
3. 输出协议：固定 JSON（relation_type/confidence/evidence_summary/decision_reason）。
4. 扩展要求：保留后续接入 OpenAI 等模型做横评与切换的技术口子。

本轮已落地的关键改造：

1. 新增 elation 路由配置（默认指向 4-pro）：
   - 文件：pps/web/lib/llm/provider.ts
2. 文本生成链路从“deepseek-only”升级为“provider 可切换”：
   - 当前支持按 provider 调用 /chat/completions，为后续 OpenAI 对比评测留口。
   - 文件：pps/web/lib/llm/client.ts
3. 新增关系判型器（固定 JSON + 保守模式）：
   - 文件：pps/web/lib/graph/relation-classifier.ts
4. 内化建边主链路升级：
   - embedding 召回候选 -> LLM 判型 -> 阈值入库，并保留规则兜底。
   - 文件：pps/web/app/api/internalize/route.ts

横评结论（第一版，供执行）：

1. 先用 4-pro 做关系判型主链路，优先稳定性与语义理解。
2. 保留 provider/model 可切换参数，后续可加入 OpenAI 进行能力/成本/时延对比。
3. 第一版先保守提召回，不追求“全连”，避免图谱噪声污染 QA。

下一步（即将执行）：

1. 用你现有 S02/S03/S04 样本跑一轮重内化复测，记录边数量、类型分布、误连样例。
2. 输出第一轮评测卡（召回提升 vs 误连变化）。
3. 再和你一起讨论是否放宽阈值、是否启用 OpenAI 对照组。

## 2026-05-29｜Phase5A 首轮 A/B 评测（用户提供 graph JSON）

评测输入：
- A: innex-graph-1780061894323.json
- B: innex-graph-1780063891996.json

核心结果：
1. 节点/边：A=4/1，B=11/4。
2. 关系类型：
   - A: supports=1
   - B: elated=1, supports=1, example_of=2
3. 置信度分布（B）：high=2, mid=2, low=0。
4. 结论：
   - 召回有提升（边数增加）。
   - 目前边证据仍主要来自 structured_overlap_fallback，说明 LLM 判型增益尚未在这批样本里充分释放。
   - 观察到至少 1 条“语义偏弱但被连上”的风险边（PRD 模板 -> 意图识别，example_of，0.6），需在保守模式下继续收紧。

下一步建议：
1. 第二轮将 example_of 的保守阈值单独提高（例如 >=0.68）。
2. 对“仅共享泛词（如 产品经理）”的候选增加惩罚。
3. 加一条日志字段，区分本边最终来源：llm_decision vs allback，便于后续横评。

## 2026-05-29｜Benchmark v0 补充：11条样本金标连边与调优策略

本次根据用户反馈“该连未连、弱相关被判强”补充了 benchmark 金标：

1. 在 docs/第二版优化-benchmark-v0.md 新增“11条样本人工金标连边（用于评测）”。
2. 明确每组 pair 的预期强度（强/中/弱）与建议关系类型（related/supports/example_of/none）。
3. 给出主评测集合，支持同时评估召回与误连。
4. 新增针对性优化策略：
   - 候选召回扩一档
   - 关系类型分阈值
   - 泛词降权
   - 证据约束
   - LLM与fallback分层统计

目标：让后续 Phase5A/5B 评测口径从“看图感觉”变成“可复跑、可量化对比”。

## 2026-05-29｜Benchmark 关系口径修正（用户确认）

口径调整：
1. 关系判定以“内容语义”为准，不再以“同平台”作为不连边依据。
2. `S01/S07/S09` 从“同平台能力验证组”调整为“Agent 主题相关组”，预期弱到中等 `related`。
3. `S11` 明确为独立样本，默认不参与跨样本建边要求。

已更新文档：
- `docs/第二版优化-benchmark-v0.md`
